# Dockerlike Names Generator

 .NET nuget to generate semi-random names that look like Docker container names.

 Just install the NuGet and use it to generate a name by calling the static method GenerateName.
